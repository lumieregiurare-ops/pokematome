{
  "name": "pokematome",
  "version": "1.0.0",
  "description": "ポケモン関連のニュースを集めてまとめる静的サイト",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "collect": "node scripts/collect.mjs",
    "start": "npm run build && node server.mjs",
    "dex": "node scripts/build-dex.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "sharp": "^0.33.5"
  }
}
